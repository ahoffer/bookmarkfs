# Expose only health (and optionally info) endpoints
management.endpoints.web.exposure.include=health,info

# Optionally change the base path for endpoints
management.endpoints.web.base-path=/actuator

# Optional: Make the health endpoint always return UP even if some components fail
 management.endpoint.health.show-details=always